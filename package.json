{
  "name": "create-typescript-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.ts",
  "scripts": {
    "start": "yarn env-cmd ts-node src/app.ts",
    "test": "yarn env-cmd ts-mocha test/**/test.spec.ts",
    "lint": "eslint \"{src,test}/**/*.ts\""
  },
  "author": "eodeluga",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^*",
    "@types/mocha": "^*",
    "@types/node": "^*",
    "@typescript-eslint/eslint-plugin": "^*",
    "@typescript-eslint/parser": "^*",
    "chai": "^*",
    "env-cmd": "^*",
    "eslint": "^*",
    "mocha": "^*",
    "ts-mocha": "^*",
    "ts-node": "^*",
    "typescript": "^*",
    "yarn": "^*"
  }
}
